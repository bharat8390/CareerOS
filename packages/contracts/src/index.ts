/**
 * CareerOS API contracts.
 *
 * The typed API client and request/response models are generated from the backend
 * OpenAPI document by the codegen step introduced in Story S1-7. Until then this
 * package exposes only its identifier so it is importable and type-checks cleanly.
 */
export const CONTRACTS_PACKAGE = 'contracts' as const;
