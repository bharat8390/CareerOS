/**
 * CareerOS design tokens.
 *
 * The token set (color, spacing, typography, radius) for the light/dark themes is
 * defined alongside the frontend shell in Story S1-6. Until then this package exposes
 * only its identifier so it is importable and type-checks cleanly.
 */
export const UI_TOKENS_PACKAGE = 'ui-tokens' as const;
