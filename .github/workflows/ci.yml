S1-5 done and CI-green. PR #8: https://github.com/bharat8390/CareerOS/pull/8

- SQLAlchemy 2.0 **async** base (typed `Base` + naming convention), `AsyncEngine`/`async_sessionmaker`, `get_db()` dependency; driver `asyncpg`.
- `DATABASE_URL` (optional) added; when set, `/ready` runs a `database` check → 200 up / **503 envelope** down.
- Alembic (async) + baseline migration: extensions `pgcrypto`/`citext`/`pg_trgm` + `feature_flags` and `app_meta` tables (no domain tables).
- Verified on a real **Postgres 16**: `upgrade → downgrade → upgrade` all clean. 28 tests pass with a live DB; 26 pass / 2 skip hermetic; ruff + mypy strict green.

**One manual step (same workflow-scope limit as S1-2):** the AC wants the migration validated against a Postgres service container in CI, which requires editing `.github/workflows/ci.yml` — I can't push workflow files. I've attached the updated `ci.yml` (adds a `postgres:16` service + a `DATABASE_URL` env + an `alembic upgrade→downgrade→upgrade` step). To apply: open `.github/workflows/ci.yml` on GitHub → Edit → replace with the attached contents → commit. Until then CI runs the hermetic suite (integration tests auto-skip), which is green.

Then merge PR #8 (Squash and merge) and reply **approve** to start S1-6 (frontend shell + design tokens + light/dark). Note: S1-6 is a frontend/UI story, so after it I'll offer to test it end-to-end in the browser.

ATTACHMENT:{"url":"https://app.devin.ai/attachments/9af54c95-37a5-4857-80ec-195f6aef32f2/careeros-ci-s1-5.yml","fileSize":2545}
